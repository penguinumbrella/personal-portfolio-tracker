/** Environment-specific configuration for the default (non-production) build. */
export const environment = {
  production: false,
  //baseApiUrl: 'http://localhost:8080/v1',
  baseApiUrl: 'https://d1jcki4jtvzqdz.cloudfront.net/v1',
};
