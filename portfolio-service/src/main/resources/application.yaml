spring:
    application:
        name: user-service

server:
    port: 8082

eureka:
    client:
        service-url:
            default-zone: http://localhost:8671
    instance:
        leaseRenewalIntervalInSeconds: 30
