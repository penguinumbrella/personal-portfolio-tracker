# user-service has been merged into `api-gateway`
#
# Run `api-gateway` (port 8125) for:
# - /v1/auth/** and /v1/users/** (local controllers + Spring Security)
# - gateway proxy to portfolio-service for /v1/investments/**, /v1/securities/**, /v1/holdings/**
#
# Do not start this project anymore.
